#posts-show.content
  .row
    .span16
      .post-component
        .post(data-post-id='#{post._id}')
          h1 #{post.title}
          .body
            #{post.body}
          .tags
            - each tagName in post.tag_names
              span.tag-name.label
                a(href='/t/#{tagName}') #{tagName}
          .footer
            span.datetime(title='#{post.created_at}')
              #{post.created_at}
            span
              #{post.comments.length}コメント
            ul.menu
              li
                a(href='#', class='new-comment') 返信
              li
                a(href='#', class='new-tag') タグを追加
          ul.comments

include ../includes/_comment
include ../includes/_tag
