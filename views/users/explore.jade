#explore.content
  .page-header
    h1 Explore
    p ポストにたくさん付けられているタグを順に表示しています。
  .row
    .span16
      - if (tags.length > 0)
        ol#tags
          - each tag in tags
            li
              span.posts-count= tag.posts_count
              a(href='/t/' + tag.name)= tag.name
