#tags-show.content
  .page-header
    h1 タグ: #{tag.name}
    form
      input(class='csrf', name='_csrf', type='hidden', value='#{csrfToken}')
      input(id='tag-lower-name', type='hidden', value='#{tag.lowerName}')
      textarea(id='post')
      button(id='create', class='btn primary') 投稿
  .row
    .span16
      .post-component
include ../includes/_timeline
